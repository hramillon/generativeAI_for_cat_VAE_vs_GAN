model_params:
  latent_dim: 10
  conv_filters: [32, 64, 128]
  kernel_size: 3

training_params:
  learning_rate: 0.001
  epochs: 10
  batch_size: 100

paths:
  save_encoder: "training/vae_mnist.keras"
  save_decoder: "training/decoder_vae.keras"